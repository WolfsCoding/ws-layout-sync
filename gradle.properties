pluginVersion=0.1.0
# WebStorm build the plugin is compiled against. Override with -PlocalIdePath=<install dir> to compile
# against a locally installed WebStorm instead of downloading one.
platformVersion=2026.2.2

kotlin.stdlib.default.dependency=false
org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
